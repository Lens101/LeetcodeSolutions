#Reverse an array, es6 destructuring example commented, simple temp variable used.

/**
 * @param {character[]} s
 * @return {void} Do not return anything, modify s in-place instead.
 */
var reverseString = function(s) {
    
    let left = 0, right = s.length-1;
let temp;
    
    while(left < right){
        //[s[left], s[right]] = [s[right], s[left]];
        let temp;
        s[temp] = s[left];
        s[left] = s[right];
        s[right] = s[temp];
        left++;
        right--;
    }
}
